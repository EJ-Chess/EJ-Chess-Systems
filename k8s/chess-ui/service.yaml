apiVersion: v1
kind: Service
metadata:
  name: chess-ui
  namespace: chess
spec:
  type: NodePort
  selector:
    app: chess-ui
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    name: http
